import React from 'react';

import DefaultLayout from '@layouts/Default';

const explorer = () => {
  return (
    <DefaultLayout>
      <div>explorer</div>
    </DefaultLayout>
  );
};

export default explorer;
