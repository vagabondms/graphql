import styled from 'styled-components';

const Main = styled.main`
  width: calc(100% - 40px);
  max-width: 935px;
  padding: calc(var(--desktop-nav-height) + 30px) 20px 0px 20px;
  margin: 0 auto 30px;
`;

export default Main;
